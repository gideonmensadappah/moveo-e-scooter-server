export enum FailureStatus {
  OPEN = 'open',
  CARE = 'care',
  CLOSED = 'closed',
}

export enum Type {
  ROUTINE = 'routine',
  CARE = 'care',
  BRAKE_REPLACEMENT = 'brake replacement',
  WHEEL_REPLACEMENT = 'wheel_replacement',
}
